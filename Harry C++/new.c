#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main()
{
    int a[1000];
    int i = 0, j = 1;
    do
    {
        cin >> k;
        k = input() if k != 'STOP' : a[i] = int(k)
                i = i + 1 elif k == 'STOP' : break else : continue
    }while(k!='STOP')
for l in range(i+1):
    j=j*l
print(j) 
return 0;
}
